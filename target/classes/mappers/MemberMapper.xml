<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.soft.study.mapper.MemberMapper">


    <select id="testConnection" resultType="string">
        SELECT 'tes123t'
    </select>
    
    <select id="checkId" parameterType="String" resultType="Integer">
    	SELECT COUNT(*) FROM STUDY_USERS WHERE USER_ID = #{param}
    </select>
    
    <select id="checkNickName" parameterType="String" resultType="Integer">
    	SELECT COUNT(*) FROM STUDY_USERS WHERE USER_NICKNAME = #{param}
    </select>
    
    <insert id="register" parameterType="MemberRegisterDTO">
    	INSERT INTO STUDY_USERS (USER_ID, USER_PW, USER_NICKNAME, USER_CREATED_AT)
    	VALUES ( #{userId}, #{userPw}, #{userNickName}, SYSDATE())
    </insert>
    
    <select id="login" parameterType="String" resultType="String">
    	SELECT USER_PW FROM STUDY_USERS WHERE USER_ID = #{param}
    </select>
    
    <select id="userInfo" parameterType="String" resultType="MemberInfoDTO">
    	SELECT
    		USER_IDX,
    		USER_ID,
    		USER_NICKNAME
   		FROM STUDY_USERS
   		WHERE USER_ID = #{param}
    </select>
</mapper>