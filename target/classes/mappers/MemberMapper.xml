<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.soft.study.mapper.MemberMapper">


    <select id="testConnection" resultType="string">
        SELECT 'tes123t'
    </select>
    
    <insert id="register" parameterType="MemberRegisterDTO">
    	INSERT INTO STUDY_USERS (USER_ID, USER_PW, NICKNAME, CREATED_AT)
    	VALUES ( #{userId}, #{userPw}, #{nickName}, SYSDATE())
    </insert>
</mapper>